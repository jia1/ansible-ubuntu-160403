---
- name: Install vim
  apt:
    name: vim

- name: Get username
  command: whoami
  register: username
  delegate_to: 127.0.0.1

- name: Download .vimrc
  get_url:
    url: "{{ url }}"
    dest: "/home/{{ username.stdout }}/.vimrc"
    force: yes
...